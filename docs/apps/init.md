**App `init`**

Responsabilidade
- Contém código legado e exemplos comentados. Não é usado ativamente pelas rotas principais (`main`, `agenda`, `checklist`).

Observações
- Muitos modelos e views estão comentados. Mantive-os como referência histórica; não são carregados no `INSTALLED_APPS` atual.
